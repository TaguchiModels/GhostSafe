<Page x:Class="GhostSafe.PageLogin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:local="clr-namespace:GhostSafe"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="PageLogin">

    <materialDesign:DialogHost Identifier="RootDialog">

        <Grid Name="MainGrid">
            <!-- 背景画像 -->
            <Image Source="Images/bridge-9599215_1280.jpg"
                   Stretch="UniformToFill"
                   Grid.RowSpan="4"
                   Panel.ZIndex="0"/>

            <!-- 半透明の紫色オーバーレイ -->
            <Rectangle Fill="#50837AE5"
                       Grid.RowSpan="4"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Panel.ZIndex="1"/>

            <!-- UIコンテンツ（最前面） -->
            <Grid Background="Transparent"
                  Grid.RowSpan="4"
                  Panel.ZIndex="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- PasswordBox -->
                <Grid Grid.Row="1">
                    <PasswordBox FontSize="14"
                                 Name="passwordBox" 
                                 Style="{StaticResource MaterialDesignFilledPasswordBox}"
                                 MinWidth="200" Margin="10" HorizontalAlignment="Center"
                                 KeyDown="passwordBox_KeyDown"
                                 Background="Transparent"
                                 materialDesign:HintAssist.Background="Transparent"
                                 materialDesign:HintAssist.Hint="{DynamicResource PasswordAuth}" />
                </Grid>

                <!-- TextBlock -->
                <Grid Grid.Row="2">
                    <TextBlock x:Name="ResultMessage"
                               Text="{DynamicResource PasswordEnterKey}"
                               TextAlignment="Center"
                               Margin="5" FontSize="16" Foreground="WhiteSmoke">
                    </TextBlock>
                </Grid>

                <Grid Grid.Row="3">
                    <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Bottom"
                       Margin="0,0,0,10"
                       FontSize="14" Foreground="WhiteSmoke">
                        <Hyperlink NavigateUri="https://pixabay.com/ja/photos/%E6%A9%8B-%E5%B7%9D-%E9%A2%A8%E6%99%AF-%E9%9B%B2-%E5%85%AC%E5%9C%92-9599215/"
                                   RequestNavigate="Hyperlink_RequestNavigate"
                                   Foreground="DeepPink">
                            橋-川-風景-雲-公園 by PIXABAY
                        </Hyperlink>
                    </TextBlock>
                </Grid>

            </Grid>
        </Grid>

    </materialDesign:DialogHost>

</Page>
